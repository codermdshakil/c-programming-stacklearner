#include <stdio.h>

int main()
{
    const float PI = 3.1416f; // using const we can make constant cariable
    printf("%f \n", PI);
    return 0;
}

// All Capital letter use for constant variable name
// Constant value need some times then we are use const
// - using const we can make constant cariable
// we can't use reserved keyword as a variable name