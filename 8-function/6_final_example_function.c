#include <stdio.h>
// Prototype 
int sum(int a, int b);
int sub(int a, int b);
void print(int a, int b, char sign);

// create sum function to calculate 2 numbers sum
int sum(int a, int b)
{
    int result = a + b;
    return result;
}
// create sum function to calculate 2 numbers sum
int sub(int a, int b)
{
    int result = a - b;
    return result;
}

int main()
{
    // We have to create a calculator that can calculate sub and sum. The print the result structured way.

    return 0;
}