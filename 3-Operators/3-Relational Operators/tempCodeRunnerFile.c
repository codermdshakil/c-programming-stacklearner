 int a_is_greater = a > b;
    printf("a is greater then b = %d\n", a_is_greater); // 1

    // < smaller then
    int a_is_smaller = a < b;
    printf("a is smaller then b = %d\n", a_is_smaller); // 0

    // == equal to
    int a_is_b = a == b;
    printf("a is b = %d\n", a_is_b); // 0

    // !=  not equal
    int a_is_not_b = a != b;
    printf("a is not b = %d\n", a_is_not_b); // 1